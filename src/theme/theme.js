import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: '#0088cc',
    },
    secondary: {
      main: '#ffcc00',
    },
    background: {
        default: 'red',
    },
  },

});

export default theme;